server:
  port: 8080
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8099/eureka/
      username: root
      password: secret
# Automatically maps original routes through gateway, NOT CUSTOM ones
#spring:
#  cloud:
#    gateway:
#      discovery:
#        locator:
#          enabled: true
#          lowerCaseServiceId: true 